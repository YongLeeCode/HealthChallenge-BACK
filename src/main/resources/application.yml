server:
  error:
    include-message: always
  port: 80

spring:
  application:
    name: health-mate
  datasource:
    username: root
    password: yong311
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/health_mate
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: false
        format_sql: false
        use_sql_comments: false
        dialect: org.hibernate.dialect.MySQLDialect
        highlight_sql: false
